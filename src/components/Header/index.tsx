import style from "./Header.module.css"

export const Header = () => {
  return (
    <div className={style.header_main}>
      <h1 className={style.title_header}>Markdown Link Analyzer</h1>
    </div>
  )}