import style from "./DirectoryView.module.css"

export const DirectoryPage = () => {
  return (
    <section id="directory" data-route="/directory" className={style.directory_main}>
        directory_main view
    </section>
  )}